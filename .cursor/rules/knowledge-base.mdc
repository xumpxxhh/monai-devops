---
description: 仅在需要业务/跨模块上下文时按 knowledge 路由加载；小改动可跳过 KB
alwaysApply: true
---

# 项目知识库使用协议

## 0. 先判断：要不要读 KB（默认多数情况跳过）

**不是所有变更都需要阅读知识库。** 先判断任务类型，再决定是否打开 `knowledge/`。

### 可跳过（直接改代码，不要先读 ROUTING）

- 纯样式 / 文案 / 无障碍标签 / i18n 措辞
- 单文件 typo、格式化、eslint/prettier 修复
- 测试断言微调、补测与当前行为对齐（不改业务语义）
- 依赖锁同步、与业务无关的脚本/CI/配置噪音
- 同文件内、无行为变化的纯重构（改名已由用户明确范围且不跨模块契约）
- 用户已给出确切文件路径与改法，且不涉及状态/协议/兼容/跨应用边界

不确定是否可跳过时：用一句话说明假设；若可能影响业务语义或跨模块契约，按下方「需要读」处理。

### 需要读 KB（先 ROUTING，再渐进加载）

- 需求理解、方案选择、跨模块 / 跨应用改动
- 业务规则、状态机、兼容历史、幂等/校验位置等语义决策
- 接口 / 消息 / 插件契约 / 资源模型等边界变更
- 用户明确要求查知识库或回补知识

## 1. 需要读时怎么读

1. **先读** `knowledge/ROUTING.md`，按线索定位应用与入口；禁止全量扫描 `knowledge/`。
2. 再读对应 `knowledge/applications/<app>/INDEX.md` 与应用总览，按需打开 product / base / tech。
3. **易变事实必须回代码核对**（接口签名、字段、枚举、开关、Topic 等）；KB 只提供稳定语境与定位入口。
4. 未确认结论只写入 `knowledge/candidate/`（或 `personal/`），不得当作事实写进 `main/` / `applications/` 正式正文。
5. 仅当本轮确认了可复用的稳定新认知时：用 `knowledge/template/candidate.md` 回补；小改动跳过 KB 的，通常也无需回补。

权威说明见 `knowledge/README.md` 与 `knowledge/KNOWLEDGE-RULES.md`。

业务沉淀请**显式**触发 `kb-iterate`（assess → 确认 → apply），勿自动写库或默认改 official。
